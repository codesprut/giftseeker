{
  "name": "GiftSeeker",
  "productName": "GiftSeeker",
  "version": "1.0.0",
  "description": "Steam raffles autojoiner",
  "main": "giftseeker.js",
  "dependencies": {},
  "devDependencies": {
    "electron": "^1.7.8"
  },
  "author": "Alexandr Pinashin",
  "license": "ISC",
  "scripts": {
    "start": "electron giftseeker.js",
    "build": "electron-packager . GiftSeeker --platform=win32 --arch=x64 --version=1.7.8 --app-version=0.1.0 --out=dist --ignore=dist --prune --asar --icon=icon.ico --overwrite"
  }
}
